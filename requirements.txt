# High Capacity Image Steganography Project Requirements
# Generated for GitHub public repository

# Core scientific computing libraries
numpy>=1.21.0
scipy>=1.7.0

# Image processing libraries
Pillow>=8.3.0
opencv-python>=4.5.0
imageio>=2.9.0
scikit-image>=0.18.0
tifffile>=2021.0.0

# Data handling and compression
lzma>=0.0.1  # Built-in Python library for .xz compression

# Data analysis and visualization (optional, for analysis scripts)
matplotlib>=3.4.0
pandas>=1.3.0

# Third-party steganography library (optional, for comparison)
stego-lsb>=1.6.0

# Standard library imports used in the project (no need to install)
# - pathlib (built into Python 3.4+)
# - csv (built into Python)
# - math (built into Python)
# - zlib (built into Python)

# Development and testing dependencies
pytest>=6.2.0  # For running tests if added

# Compatibility notes:
# - Python 3.8+ recommended
# - Tested on Windows, Linux, and macOS